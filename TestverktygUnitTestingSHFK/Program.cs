using System;
using System.Collections.Generic;

namespace TestverktygUnitTestingSHFK
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Random random = new Random();
            List<int> newAccounts = new List<int>();
            bool unique = true;

            for (int i = 0; i < 1000; i++)
            {
                int newNumber = random.Next(1000, 2001);
                Console.WriteLine(newNumber);
                newAccounts.Add(newNumber);

                for (int j = 0; j < newAccounts.Count; j++)
                {
                    if (newAccounts[j] == newAccounts[i] && j != i)
                    {
                        unique = false;
                        break;
                    }
                }
                if (!unique)
                {
                    Console.WriteLine($"Samma siffra i iteration {i}: " + newAccounts[i]);
                    break;
                }
            }
                */
        }
    }
}
